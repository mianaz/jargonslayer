# 复制为 .env.local 后填入，可让服务端默认使用该 Key（无需在 UI 里填）。
# 也可以不配这里，直接在网页 设置 → AI 检测 → API Key 填入（只存本机浏览器）。
# 从 https://console.anthropic.com/ 获取。
ANTHROPIC_API_KEY=

# 可选：覆盖默认模型（UI 设置也能改）
# JARGONSLAYER_DETECT_MODEL=claude-haiku-4-5
# JARGONSLAYER_SUMMARY_MODEL=claude-sonnet-5
# JARGONSLAYER_TRANSLATE_MODEL=claude-haiku-4-5

# 订阅直连（实验性，v0.2.2，仅本地开发档）：用你自己机器上已登录的
# claude/codex CLI 调用 detect/define，凭据不经过任何服务器，见
# sidecar/agent_server.py 与 README「订阅直连」章节。设为 1 才会在构建产物中
# 出现该功能（设置区块 + 调用分支）；不设或留空则该功能完全不出现在产物里
# （连同 UI 文案与调用代码一并被构建工具剔除）。体验版构建绝不设置此项。
# NEXT_PUBLIC_ENABLE_SUBSCRIPTION_DIRECT=1

# 可选：远程熔断开关（kill switch 第三层）的 flags.json 地址，未设则默认指向
# 项目自带的 GitHub Pages 落地页（该文件本身需另行发布；404/超时按“允许”处理，
# 不会因该文件缺失而锁死已启用此功能的用户）。
# NEXT_PUBLIC_SUBSCRIPTION_FLAGS_URL=https://mianaz.github.io/jargonslayer/flags.json

# 仅限体验版托管部署（oracle）使用：设为 1 后，服务端拒绝任何携带自带 Key 请求头
# 的 detect/define/translate/summarize/correct 调用（400），从而让按 IP 限流 /
# 每日预算生效——这两项限制平时只在服务端用自己的共享 Key 时才启用，见
# lib/llm/anthropic.ts 的 rejectClientCreds。规范值为 "1"（也接受 "true"）。
# 自建 / 桌面版不要设置，会导致自带 Key 功能被整体拒绝。
# JARGONSLAYER_SHARED_KEY_ONLY=1
